import { get, ref } from 'firebase/database'
import { rtdb } from '../config/firebase'
import { rankSearch } from '../utils/search'
const TYPES=['clients','projects','accounts','income','expenses','projectDocuments','credentials'];const names={clients:'Clients',projects:'Projects',accounts:'Accounts',income:'Income',expenses:'Expenses',projectDocuments:'Documents',credentials:'Credentials'};
export const resultPath=(type,item)=>type==='clients'?`/clients/${item.id}`:type==='projects'?`/projects/${item.id}`:type==='accounts'?`/accounts/${item.id}`:type==='income'&&item.sourceType==='Project Payment'?`/projects/${item.projectId}?tab=payments`:type==='income'?`/income/${item.id}/edit`:type==='expenses'?`/expenses/${item.id}`:type==='projectDocuments'?`/projects/${item.projectId}?tab=documents&document=${item.id}`:type==='credentials'?`/credentials?credential=${item.id}`:'/dashboard'
export async function universalSearch(uid,raw,type='All'){const term=String(raw||'').trim().toLowerCase();if(term.length<2)return[];const collections=type==='All'?TYPES:[type];const values=await Promise.all(collections.map(async name=>{const snap=await get(ref(rtdb,`users/${uid}/${name}`));return Object.entries(snap.val()||{}).map(([id,data])=>({id,...data,entityType:name,entityLabel:names[name]})).filter(item=>rankSearch(item,term)>0).map(item=>({...item,path:resultPath(name,item)}))}));return values.flat().sort((a,b)=>rankSearch(b,term)-rankSearch(a,term)).slice(0,40)}
