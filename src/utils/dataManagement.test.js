import { describe, expect, it } from 'vitest'
import { safeExportFilename, validateImport } from './importValidation'
describe('data management validation', () => { const existing = { Clients: [{ name: 'Acme' }], clientIds: new Set(['c1']), accountIds: new Set(['a1']), projectIds: new Set(['p1']) }; it('accepts a valid client row', () => expect(validateImport('Clients', [{ Name: 'New Client', 'Client Type': 'Business', Status: 'Active' }], existing).errors).toEqual([])); it('rejects duplicate and missing client names', () => expect(validateImport('Clients', [{ Name: 'Acme', 'Client Type': 'Business', Status: 'Active' }, { Name: '', 'Client Type': 'Business', Status: 'Active' }], existing).errors).toHaveLength(2)); it('rejects invalid amount and date', () => expect(validateImport('Income', [{ Title: 'Fee', Amount: 'bad', 'Account ID': 'a1', Currency: 'INR', 'Received Date': '2026-99-99', 'Income Category': 'Work', 'Payment Mode': 'Bank', 'Source Type': 'Independent Income' }], existing).errors[0].reasons.join(' ')).toMatch(/Amount|Received Date/)); it('protects relationship references', () => expect(validateImport('Projects', [{ Name: 'Project', 'Client ID': 'missing', 'Project Type': 'Web', Status: 'Planning', Currency: 'INR', 'Total Amount': '100', 'Start Date': '2026-08-22' }], existing).errors[0].reasons[0]).toMatch(/Client ID/)); it('uses safe filenames', () => expect(safeExportFilename('Income', 'xlsx')).toMatch(/^Income_Export_.*\.xlsx$/)); })
