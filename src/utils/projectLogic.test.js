import { describe, expect, it } from 'vitest'
import { formatCurrency, safeAddMinorUnits, safeSubtractMinorUnits, toMinorUnits } from './money'
import { deadlineState, initializeFinancials, summarizeProjects, transitionProjectStatus } from './projectLogic'
describe('money', () => { it('uses integer minor units', () => { expect(toMinorUnits('40000.50')).toBe(4000050); expect(toMinorUnits('-1')).toBeNull(); expect(safeAddMinorUnits(2, 3)).toBe(5); expect(safeSubtractMinorUnits(2, 3)).toBe(0) }); it('formats safely', () => expect(formatCurrency(4000050, 'INR', 'en-IN')).toContain('40,000.50')) })
describe('project logic', () => { it('closes and reopens priorities', () => { expect(transitionProjectStatus('In Progress', 'Completed', 'High').priority).toBe('Not Applicable'); expect(transitionProjectStatus('Completed', 'In Progress', 'Not Applicable').priority).toBe('Medium') }); it('initializes financial fields', () => expect(initializeFinancials(1000)).toMatchObject({ receivedAmountMinor: 0, remainingAmountMinor: 1000, paymentStatus: 'Not Started' })); it('calculates deadline', () => expect(deadlineState('2020-01-01', 'In Progress', new Date('2020-01-02'))).toBe('Overdue')); it('aggregates project summaries', () => expect(summarizeProjects([{ status: 'Completed', totalAmountMinor: 100, receivedAmountMinor: 0, remainingAmountMinor: 100 }]).remaining).toBe(100)) })
